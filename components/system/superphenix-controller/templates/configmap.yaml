apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "superphenix-controller.fullname" . }}
  labels:
    {{- include "superphenix-controller.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- toYaml .Values.superphenixController.config | nindent 4 }}

