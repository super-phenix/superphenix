apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "superphenix-api.fullname" . }}
  labels:
    {{- include "superphenix-api.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- toYaml .Values.superphenixApi.config | nindent 4 }}
