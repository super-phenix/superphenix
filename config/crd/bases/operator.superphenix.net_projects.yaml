---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: projects.operator.superphenix.net
spec:
  group: operator.superphenix.net
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.organizationRef.name
      name: Organization
      type: string
    - jsonPath: .spec.projectID
      name: ProjectID
      type: string
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .status.conditions[?(@.type=="Bound")].status
      name: Bound
      type: string
    - jsonPath: .status.availableZones
      name: Available AZs
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project represents a Superphenix project onboarded via GitOps.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of Project.
            properties:
              availabilityZones:
                description: AvailabilityZones references the Cluster resources where
                  this project can exist.
                items:
                  description: AvailabilityZoneReference defines a reference to a
                    Cluster resource.
                  properties:
                    name:
                      description: Name of the Cluster.
                      type: string
                    namespace:
                      description: Namespace of the Cluster. If empty, the namespace
                        of the Project is used.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              gitops:
                description: GitOps defines the GitOps application configuration for
                  this project.
                properties:
                  manifestLocation:
                    description: ManifestLocation defines where the GitOps manifests
                      are located.
                    properties:
                      path:
                        description: Path overrides the default path.
                        type: string
                      repoURL:
                        description: RepoURL overrides the default repository URL.
                        type: string
                      targetRevision:
                        description: TargetRevision overrides the default target revision.
                        type: string
                    type: object
                  valuesLocation:
                    description: ValuesLocation defines where the GitOps values are
                      located.
                    properties:
                      credentials:
                        description: Credentials defines the Git credentials for the
                          values repository.
                        properties:
                          enableLfs:
                            description: EnableLfs enables Git LFS.
                            type: boolean
                          forceHttpBasicAuth:
                            description: ForceHttpBasicAuth forces HTTP Basic Auth.
                            type: boolean
                          insecure:
                            description: Insecure allows connecting to an insecure
                              Git repository.
                            type: boolean
                          password:
                            description: Password for Git authentication.
                            type: string
                          sshPrivateKey:
                            description: SshPrivateKey for SSH authentication.
                            type: string
                          username:
                            description: Username for Git authentication.
                            type: string
                        type: object
                      path:
                        description: Path overrides the default path for values.
                        type: string
                      repoURL:
                        description: RepoURL overrides the default repository URL
                          for values.
                        type: string
                      targetRevision:
                        description: TargetRevision overrides the default target revision
                          for values.
                        type: string
                    type: object
                type: object
              name:
                description: |-
                  Name allows overriding the name used in labels.
                  It must be a valid Kubernetes label value (RFC 1123/6399).
                maxLength: 63
                pattern: ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$
                type: string
              organizationRef:
                description: OrganizationRef references the Organization resource
                  this project belongs to.
                properties:
                  name:
                    description: Name of the Organization.
                    type: string
                  namespace:
                    description: Namespace of the Organization. If empty, the namespace
                      of the Project is used.
                    type: string
                required:
                - name
                type: object
              projectID:
                description: |-
                  ProjectID is the Superphenix project UUID.
                  It must be a valid UUIDv4.
                pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                type: string
            required:
            - organizationRef
            - projectID
            type: object
          status:
            description: Status defines the observed state of Project.
            properties:
              availableZones:
                description: |-
                  AvailableZones lists the names of availability zones where the project is available.
                  Format: "region-availabilityZone"
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the current state of the Project
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed Project.
                format: int64
                type: integer
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
